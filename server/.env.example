NODE_ENV=development
PORT=5000
MONGO_URI=mongodb://localhost:27017/mediguide
# Required — the server will refuse to start if this is missing. Use a long random string (e.g. openssl rand -hex 64).
JWT_SECRET=your_super_secret_jwt_key_here
